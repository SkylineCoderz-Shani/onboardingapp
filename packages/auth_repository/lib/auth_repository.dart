library auth_repository;

/// A Calculator.
/// Too keep separate file of the cloud functions
/// It contain all authentication and authorization functions which we can use in other application as well
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;
}
